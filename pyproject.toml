[project]
name = "mcp-server-nm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dbus-python>=1.4.0",
    "mcp>=1.10.0",
]
